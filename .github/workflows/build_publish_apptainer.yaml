name: Apptainer Build and Publish

on:
  push:
    branches:
      - apptainer

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - uses: eWaterCycle/setup-apptainer@v2
      with:
        apptainer-version: 1.3.0

    - name: Build SIF image
      run: |
        apptainer build voluseg_latest.sif docker://ghcr.io/mikarubi/voluseg/voluseg:latest

    - name: Login and Deploy Container
      run: |
          echo ${{ secrets.GITHUB_TOKEN }} | apptainer registry login -u ${{ github.actor }} --password-stdin oras://ghcr.io
          apptainer push voluseg_latest.sif oras://ghcr.io/${{ github.repository }}/voluseg_apptainer:latest